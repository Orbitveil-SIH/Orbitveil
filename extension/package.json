{
  "name": "extension",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/background/service-worker.js --bundle --outfile=dist/service-worker.js --format=esm --target=chrome100 && esbuild src/offscreen/offscreen.js --bundle --outfile=dist/offscreen.js --format=esm --target=chrome100"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}